<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ligg.mapper.user.UserChatMessageMapper">

    <select id="getAllChatMessage" resultType="com.ligg.dto.UserChatMessageDto">
        select cm.id,cm.content,cm.send_time,cm.to_user as username,
               u.nickname,u.user_pic
        from chat_message cm
        join user u on cm.to_user = u.username
        where cm.from_user = #{username}

    </select>
</mapper>
